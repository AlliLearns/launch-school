// Take your code from the previous exercese 
// and replace it with a ternary operator. 

let randomNumber = Math.round(Math.random());

// randomNumber === 1 ? console.log("Yes!") : console.log("No.");

console.log(randomNumber ? 'Yes!' : 'No.');