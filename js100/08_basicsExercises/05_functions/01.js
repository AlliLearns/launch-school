// Write the function `sum` such that it accepts 
// two arguments and returns the sum of its arguments. 

// Assuming that input is always a number
function sum(num1, num2) {
    return num1 + num2;
}

console.log(sum(22, 10));