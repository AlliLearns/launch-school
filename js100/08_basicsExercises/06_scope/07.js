// What will this code log to the console and why?

let a = 1;

function myFunction() {
    console.log(a);
}

myFunction();

// It'll log `1` to the console.
// Variable `a` is declared in the global
// scope, at the very top of our code,
// so everything has access to it.