// Write a line of code that returns the largest numeric value 
// that can be represented in JavaScript.
// (They mean in the 'number' type and not the 'bigint' type.)

console.log(Number.MAX_VALUE);
console.log(Number.MAX_SAFE_INTEGER); //Largest number that can be processed and compared with precision.
