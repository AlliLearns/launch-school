// Below is a simple object representing our dog, Fido.
// Add `age` and `favorite food` to the Fido object.

let fido = {
    name: 'Fido',
    species: 'Labrador Retriever',
    color: 'brown',
    weight: 16,
};

fido.age = 8;
fido['favorite food'] = chicken;