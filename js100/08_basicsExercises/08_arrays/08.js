// How can you check whether a variable holds a value 
// that is an array? For example, imagine you're 
// writing a function and want to check whether its argument
// is an array. 

function filter(input) {
    // Is input an array?
    if (Array.isArray(input)) {
        // do something 
    }
}