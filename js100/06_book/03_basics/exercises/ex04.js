// Explain why this code logs `510` instead of `15`: 

console.log('5' + 10);
// This code logs '510' instead of `15` because of 
// JavaScript's type-cohersion. 
// It turns a number into a string when added like this and assumes concationation.