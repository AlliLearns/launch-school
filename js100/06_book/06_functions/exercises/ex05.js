// What does the following code log to the console?

function scream(words) {
    return words + '!!!!';
}

scream('Yipeee');

// It logs nothing, there is no output.
// The new string will be returned, but 
// since we don't log that result to 
// the console, nothing happens. 